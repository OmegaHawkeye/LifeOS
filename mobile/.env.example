# On an iOS Simulator, localhost resolves to the Mac running the API.
# On a physical device, use the LifeOS server's local LAN address, e.g.
# http://192.168.1.20:8000/api/v1
EXPO_PUBLIC_LIFEOS_API_URL=http://localhost:8000/api/v1
