# LifeOS web

The LifeOS installable web application uses React, TypeScript, Vite, and Tailwind CSS.

Run commands from the repository root unless you are working on this client in isolation. The root README is the canonical setup guide.

```bash
pnpm dev
pnpm test
pnpm lint
pnpm typecheck
pnpm build
```
